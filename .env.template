# Required for video analysis (Gemini via Vertex AI)
# Run: gcloud auth login && gcloud config set project YOUR_PROJECT
# No API key needed — uses gcloud auth token

# Required for TTS narration (ElevenLabs)
ELEVENLABS_API_KEY=

# Path to source video files
MEDIA_DIR=

# Google Cloud SDK path (auto-detected on most systems)
# GCLOUD_PATH=

# Budget limit before warnings (default: $100)
# BUDGET_LIMIT_USD=100
