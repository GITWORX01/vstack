{
  "name": "vstack",
  "version": "0.1.0",
  "type": "module",
  "description": "AI-powered video production pipeline for Claude Code. Analyze, narrate, assemble, and render videos from source footage.",
  "license": "MIT",
  "scripts": {
    "setup": "node setup",
    "frame-server": "node lib/frame-server.mjs",
    "db": "node lib/db.mjs",
    "analyze": "node lib/analyze-episode.mjs",
    "batch": "node lib/batch-analyze.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
